# configs/model/image_only.yaml
# @package _global_

ablation_mode: image_only
experiment_name: ablation_image_only
output_dir: outputs/ablation_image_only

loss:
  contrastive_lambda: 0.0

training:
  modality_dropout: 0.0
