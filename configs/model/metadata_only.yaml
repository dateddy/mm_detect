# configs/model/metadata_only.yaml
# @package _global_

ablation_mode: metadata_only
experiment_name: ablation_metadata_only
output_dir: outputs/ablation_metadata_only

loss:
  contrastive_lambda: 0.0

training:
  modality_dropout: 0.0
  freeze_encoder_epochs: 0
  warmup_steps: 200
  lr_fusion: 5.0e-4
