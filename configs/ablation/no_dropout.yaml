# configs/ablation/no_dropout.yaml
# DEPRECATED: Use no_modality_dropout.yaml instead (better naming)
# Kept for backward compatibility only.
# @package _global_

ablation_mode: full_no_modality_dropout
experiment_name: ablation_no_dropout_legacy
output_dir: outputs/ablation_no_dropout_legacy

training:
  modality_dropout: 0.0
